/** Couleurs de barre d'adresse. Lues par les deux `viewport`, le script de
 *  thème et le sélecteur : des copies littérales finiraient par diverger. */
export const THEME_COLORS = { light: '#e7ecf1', dark: '#060b12' } as const;
